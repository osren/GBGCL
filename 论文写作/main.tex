% GBGCL — IEEE conference (~5 pages: content + references)
% Style aligned with SGRL (NeurIPS 2024): Introduction → Preliminary → Methodology → Experiment → Conclusion

\documentclass[10pt,conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{cuted}
\usepackage{caption}
\captionsetup{compatibility=false}
\graphicspath{{figures/}}

\renewcommand{\figurename}{Figure}
\renewcommand{\tablename}{Table}
\renewcommand{\thetable}{\arabic{table}}

\begin{document}

\title{GBGCL: Granular Ball Graph Contrastive Learning with Ball-Aware Topology Encoding}

\author{\IEEEauthorblockN{Cheng Tan\textsuperscript{*}}
\IEEEauthorblockA{\textit{Chongqing University of Posts and Telecommunications}\\
Chongqing 400065, China \\
\texttt{1529924810@qq.com}}
\thanks{*Corresponding author. E-mail: \texttt{1529924810@qq.com}.}}

\maketitle

\begin{abstract}
Graph Contrastive Learning (GCL) has become a dominant paradigm for self-supervised node representation learning.
Scattering Graph Representation Learning (SGRL) explicitly unifies representation scattering and topology constraints through a Representation Scattering Module (RSM) and a Topology-based Constraint Module (TCM), achieving strong performance without negative samples or heavy augmentations.
However, TCM propagates information only along the node adjacency, leaving meso-scale community structure underexplored.
In this paper, we propose Granular Ball Graph Contrastive Learning (GBGCL), which builds quality-guided granular balls, diffuses information on an induced ball graph, and---critically---injects ball embeddings into \emph{both} SGRL encoders via a Ball-aware Topology Convolution Module (BTCM).
BTCM replaces standard GCN messages with ball-augmented convolutions so meso-scale structure enters the same pathway optimized by RSM and TCM, rather than remaining in a weakly coupled auxiliary branch.
We extensively evaluate full GBGCL on four benchmark datasets under the SGRL protocol.
Full GBGCL consistently outperforms classic GCL baselines and slightly exceeds SGRL on every dataset (up to +0.09\,pp), while ablations confirm that BTCM---not parallel write-back alone---delivers the gain.
\end{abstract}

\begin{IEEEkeywords}
graph contrastive learning, representation scattering, granular ball computing, self-supervised learning
\end{IEEEkeywords}

%======================================================================
\section{Introduction}
\label{sec:intro}

Graph Neural Networks (GNNs) have achieved impressive performance on social, citation, and co-purchase networks~\cite{luo2024tunedgnn}, yet supervised training typically requires costly labels.
Graph Contrastive Learning (GCL)~\cite{liu2022graphssl,he2024sgrl} addresses this limitation by learning from proxy tasks defined on the graph itself.
Among recent advances, SGRL~\cite{he2024sgrl} reveals that explicit \emph{representation scattering}---pushing embeddings away from a global center while preserving topology---can match or exceed classical contrastive pipelines without negative sampling.

SGRL introduces a Representation Scattering Mechanism (RSM) on the target encoder and a Topology-based Constraint Mechanism (TCM) on the online encoder, coupled with bootstrapping-style alignment and exponential moving average (EMA) updates~\cite{thakoor2021bgrl,sun2024sgcl}.
It reports state-of-the-art node classification accuracy on homophilous benchmarks (e.g., 94.15\% on Coauthor CS and 90.23\% on Amazon Computers).
Nevertheless, TCM aggregates neighbors only at the \emph{node} level via $\hat{A}^{k}$.
Meso-scale structures---communities that sit between individual nodes and the full graph---are not modeled as first-class signals in the encoder.

Granular ball computing~\cite{wang2023gbknn,xia2025gbgc,xia2025sgbgc} partitions data into quality-controlled balls and supports multi-granularity reasoning.
A natural extension of SGRL is to construct balls from graph topology and embeddings, diffuse information on a ball graph, and couple the resulting meso-scale topology with scattering-based learning.
However, simply attaching ball diffusion as a parallel branch---through write-back or lightly weighted auxiliary losses---may fail to alter the embeddings used at test time, which in SGRL are concatenated online and predictor outputs.
Moreover, global scattering encourages dispersion while intra-ball diffusion encourages local cohesion; without routing ball structure into the main encoder pathway, these objectives can largely cancel in practice.

To address these issues, we propose \textbf{Granular Ball Graph Contrastive Learning (GBGCL)}.
GBGCL extends SGRL with (i)~quality-guided ball partitioning and $K$-step diffusion on a ball affinity graph; (ii)~convex write-back and ball-level scatter / InfoNCE losses; and (iii)~\textbf{BTCM}, a ball-aware convolution layer that replaces standard GCN messages in both encoders so meso-scale context co-trains with RSM and TCM (Figure~\ref{fig:framework}).
We make the following contributions:
\begin{itemize}
  \item We formulate GBGCL as a multi-granularity extension of SGRL and implement BTCM as a drop-in BallConv layer with node--ball lookup, applied symmetrically to online and target encoders so ball signals alter downstream probe embeddings.
  \item We provide extensive experiments on CS, Photo, Physics, and Computers under the SGRL protocol; full GBGCL surpasses SGRL by 0.05--0.09\,pp, and ablations isolate BTCM as the decisive component over parallel diffusion alone.
\end{itemize}

%======================================================================
\section{Preliminaries}
\label{sec:prelim}

\textbf{Graph data.}
A graph is $\mathcal{G}=(V,E)$ with $N$ nodes, feature matrix $X\in\mathbb{R}^{N\times d}$, and adjacency $A$.
The degree-normalized adjacency with self-loops is $\hat{A}=\tilde{D}^{-1/2}(A+I)\tilde{D}^{-1/2}$.

\textbf{Graph contrastive learning.}
Given $(X,A)$, GCL trains an encoder $f(\cdot)$ without labels to produce representations $H=f(X,A)$ applicable to downstream tasks via linear probes~\cite{luo2024tunedgnn,liu2022graphssl}.

\textbf{SGRL backbone.}
SGRL maintains online parameters $\theta$ and target parameters $\phi$.
The target encoder produces scattered representations via RSM; the online encoder produces topology-aware representations via TCM and a predictor that aligns to the target under stop-gradient~\cite{he2024sgrl}.
Parameters $\phi$ are updated by EMA at the end of each epoch.
Evaluation concatenates online and predictor embeddings for logistic regression, matching our implementation baseline.

%======================================================================
\section{Methodology}
\label{sec:method}

The preceding discussion motivates meso-granularity on top of SGRL's node-level scattering and topology constraints.
As shown in Figure~\ref{fig:framework}, GBGCL comprises two encoders $f_\theta$ and $f_\phi$ that follow the SGRL pipeline, while an additional ball module partitions nodes, diffuses on a ball graph, and---through BTCM---injects ball features into convolution.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{fig1.pdf}
  \caption{Overview of GBGCL in six stages. \textbf{(a)}~Attributed input graph $\mathcal{G}=(X,A)$.
  \textbf{(b)}~Dual SGRL encoders: online $f_\theta$ and EMA-updated target $f_\phi$, yielding $H_{\mathrm{o}}$ and $H_{\mathrm{t}}$ (red dashed arc: EMA).
  \textbf{(c)}~Quality-guided granular partitioning into balls $\mathcal{B}$ with assignment $\beta(i)$.
  \textbf{(d)}~$K$-step message passing on the induced ball graph $W$ to obtain $H_{\mathrm{ball}}$.
  \textbf{(e)}~Node write-back $z_i=\alpha h_i+(1-\alpha)z_{\mathrm{ball}}$ and BTCM injection of $b_{\beta(i)}$ into both encoders.
  \textbf{(f)}~Target RSM scatters normalized embeddings around center $\mathbf{c}$; the online branch applies TCM and BYOL-style alignment; evaluation uses $\mathrm{concat}(H_{\mathrm{o}},H_{\mathrm{pr}})$.
  The bottom bar jointly optimizes $\mathcal{L}_{\mathrm{align}}$, $\mathcal{L}_{\mathrm{ball\mbox{-}s}}$, $\mathcal{L}_{\mathrm{ball\mbox{-}nce}}$, and $\mathcal{L}_{\mathrm{RSM}}$ (dashed blue links).}
  \label{fig:framework}
\end{figure*}

\subsection{Representation Scattering and Topology Backbone}
Following SGRL~\cite{he2024sgrl}, target representations $\mathbf{h}_{\mathrm{t},i}$ are $\ell_2$-normalized to $\tilde{\mathbf{h}}_{\mathrm{t},i}$ on the hypersphere.
The scattered center is $\mathbf{c}=\frac{1}{N}\sum_i \tilde{\mathbf{h}}_{\mathrm{t},i}$, and RSM minimizes
\begin{equation}
  \mathcal{L}_{\mathrm{RSM}} = -\frac{1}{N}\sum_{i=1}^{N}\left\|\tilde{\mathbf{h}}_{\mathrm{t},i}-\mathbf{c}\right\|_2^2.
  \label{eq:rsm}
\end{equation}
On the online side, TCM aggregates $k$-order neighbors:
\begin{equation}
  H_{\mathrm{online}}^{\mathrm{topo}} = \hat{A}^{k}H_{\mathrm{online}} + H_{\mathrm{online}},
  \label{eq:tcm}
\end{equation}
and a predictor $q_\psi$ yields $Z_{\mathrm{online}}=q_\psi(H_{\mathrm{online}}^{\mathrm{topo}})$ with alignment loss
\begin{equation}
  \mathcal{L}_{\mathrm{align}} = -\frac{1}{N}\sum_{i=1}^{N}
  \frac{Z_{\mathrm{online},i}^{\top}\tilde{\mathbf{h}}_{\mathrm{t},i}}
  {\|Z_{\mathrm{online},i}\|\|\tilde{\mathbf{h}}_{\mathrm{t},i}\|}.
  \label{eq:align}
\end{equation}
Target parameters are updated by EMA: $\phi\leftarrow\tau\phi+(1-\tau)\theta$.
Eqs.~\eqref{eq:rsm}--\eqref{eq:align} mitigate adversarial interactions between scattering and topology, as in SGRL.

\subsection{Granular Ball Diffusion Module}
\textbf{Partitioning.}
A \texttt{Granular} module recursively splits nodes by quality (\texttt{homo}, \texttt{detach}, \texttt{edges}) into balls $\mathcal{B}=\{B_m\}$ with assignment $\beta(i)$.
Ball centers are $h_m^{\mathrm{ball}}=\mathrm{mean}_{i\in B_m} h_i$.

\textbf{Ball graph and diffusion.}
Affinity $W$ fuses inter-ball edges and center similarity (\texttt{topo+center}, optional $k$NN).
For $t=1,\ldots,K$,
\begin{equation}
  H^{(t)}_{\mathrm{ball}} = (1-\beta_d) H^{(t-1)}_{\mathrm{ball}} + \beta_d \tilde{D}_W^{-1} W H^{(t-1)}_{\mathrm{ball}},
  \label{eq:diffuse}
\end{equation}
with $Z_{\mathrm{ball}}=H^{(K)}_{\mathrm{ball}}$ and write-back
\begin{equation}
  z_i = \alpha h_i + (1-\alpha)\, z_{\mathrm{ball}}[\beta(i)].
  \label{eq:writeback}
\end{equation}
Balls are rebuilt every $T_{\mathrm{rebuild}}$ epochs; \texttt{gb\_incremental} refreshes centers between rebuilds.

\textbf{Ball-level losses.}
Hungarian-matched ball centers across online/target views support ball scatter $\mathcal{L}_{\mathrm{ball\mbox{-}s}}$ and InfoNCE $\mathcal{L}_{\mathrm{ball\mbox{-}nce}}$~\cite{he2024sgrl,sun2024sgcl} (weights $\lambda_{\mathrm{s}}{=}0.05$, $\lambda_{\mathrm{n}}{=}0.02$).
Write-back $z_i$ supervises these auxiliary terms; the main encoder outputs $H_{\mathrm{online}}$ and $H_{\mathrm{t}}$ are unchanged until BTCM (Sec.~\ref{sec:btcm}) augments convolution.

\subsection{Ball-aware Topology Convolution (BTCM)}
\label{sec:btcm}

Parallel write-back barely perturbs the embeddings used at test time: on Amazon Photo, $\cos(h,z_{\mathrm{new}})\approx 0.9996$ and \texttt{h\_z\_diff} is only 2--4\% without BTCM, because probes consume $\mathrm{concat}(H_{\mathrm{o}},H_{\mathrm{pr}})$ rather than $z_i$.
\textbf{BTCM} closes this gap by routing meso-scale ball context into \emph{both} SGRL encoders.

\textbf{Node--ball lookup.}
Given diffused ball embeddings $H_{\mathrm{ball}}\in\mathbb{R}^{B\times d_b}$ and assignment $\beta(\cdot)$, we form a node-level ball feature matrix $B\in\mathbb{R}^{N\times d_b}$ with rows $B_{i,:}=H_{\mathrm{ball}}[\beta(i)]$.
Between full rebuilds, we cache the latest $H_{\mathrm{ball}}$ as \texttt{prev\_H\_ball} and refresh ball centers incrementally, keeping meso-scale signals stable across epochs.

\textbf{BallConv message passing.}
Standard GCN layers in $f_\theta$ and $f_\phi$ are replaced by ball-aware convolutions.
For each edge $(i,j)\in E$,
\begin{equation}
  m_{ij} = \mathrm{PReLU}\!\Big(\mathrm{BN}\big(W_m [h_i \| h_j \| B_i \| B_j]\big)\Big),
  \label{eq:btcm}
\end{equation}
messages are mean-aggregated at each destination node, and stacked identically to the SGRL backbone ($d_b{=}1024$ by default).
Online and target encoders share the same $B$ each epoch, so RSM scattering, TCM topology aggregation, and ball context are co-optimized on the representations later concatenated for probing.

\textbf{Epoch-wise procedure.}
Each training epoch executes: (i)~forward both encoders with current $B$ (zeros before the first partition); (ii)~optionally rebuild balls and run $K$-step diffusion (Eq.~\eqref{eq:diffuse}); (iii)~update $B$ for the next epoch; (iv)~minimize $\mathcal{L}_{\mathrm{online}}$ and $\mathcal{L}_{\mathrm{RSM}}$ with EMA update on $\phi$.
\textbf{Full GBGCL} denotes SGRL + granular diffusion + ball losses + BTCM; \textbf{GBGCL w/o BTCM} disables Eq.~\eqref{eq:btcm} and retains only the parallel branch.

\subsection{Overall Objective}
The online step minimizes
\begin{equation}
  \mathcal{L}_{\mathrm{online}} = \mathcal{L}_{\mathrm{align}}
  + \lambda_{\mathrm{s}}\mathcal{L}_{\mathrm{ball\mbox{-}s}}
  + \lambda_{\mathrm{n}}\mathcal{L}_{\mathrm{ball\mbox{-}nce}},
  \label{eq:online}
\end{equation}
while the target step minimizes $\mathcal{L}_{\mathrm{RSM}}$.
With BTCM enabled, gradients from $\mathcal{L}_{\mathrm{align}}$ and ball-level losses flow through BallConv layers, directly shaping the probed embeddings.

%======================================================================
\section{Experiment}
\label{sec:experiments}

We evaluate GBGCL on four widely used benchmarks: Amazon-Computers and Amazon-Photo~\cite{luo2024tunedgnn}, Coauthor-CS (Co.CS), and Coauthor-Physics (Co.Physics).
We compare against representative self-supervised baselines reported in SGRL~\cite{he2024sgrl}: raw features, DGI, GRACE, BGRL~\cite{thakoor2021bgrl}, and MVGRL; recent graph SSL methods include GraphMAE~\cite{hou2022graphmae} and CARL-G~\cite{shiao2023carlg}.
We additionally report SGRL~\cite{he2024sgrl} and two GBGCL variants: \emph{w/o BTCM} (parallel diffusion only) and \emph{full} (with BTCM, Sec.~\ref{sec:btcm}).
Following SGRL's node classification protocol, we train a 1-layer BallConv/GCN encoder (hidden size 1024, Adam lr $10^{-3}$, 700 epochs, 5 trials, seed 66666) and evaluate with logistic regression on frozen concatenated online/predictor embeddings.
Hyperparameters are selected by grid search (Stage-A 150 ep, Stage-B 700 ep); best settings per dataset: Co.CS (detach, dot, $\alpha{=}0.3$), Photo (homo, cos, 0.3), Co.Physics (detach, dot, 0.3), Computers (homo, dot, 0.7).

\subsection{Performance Analysis}
As shown in Table~\ref{tab:main}, full GBGCL reports the best node classification F1-scores on all four benchmarks, following the same format as SGRL Table~1~\cite{he2024sgrl}.
Classic baseline numbers are taken from SGRL; SGRL and all GBGCL rows are 5-trial means $\pm$ standard deviations from our implementation under the same protocol.

\begin{strip}
  \vspace{-0.5em}
  \centering
  \footnotesize
  \setlength{\tabcolsep}{3.5pt}
  \captionof{table}{Performance on node classification. OOM signifies out-of-memory on 24\,GB GPU. $X$, $A$ denote node attributes and adjacency. Classic baselines from SGRL~\cite{he2024sgrl}; SGRL and GBGCL from our runs. Optimal results are in \textbf{bold}.}
  \label{tab:main}
  \begin{tabular}{llcccc}
    \toprule
    Method & Available Data & Computers & Photo & Co.CS & Co.Physics \\
    \midrule
    Raw Features & $X$ & 73.81$\pm$0.00 & 78.53$\pm$0.00 & 90.37$\pm$0.00 & 93.58$\pm$0.00 \\
    DGI & $X,A$ & 83.95$\pm$0.47 & 91.61$\pm$0.22 & 92.15$\pm$0.63 & 94.51$\pm$0.52 \\
    GRACE & $X,A$ & 86.50$\pm$0.33 & 92.46$\pm$0.18 & 92.17$\pm$0.04 & OOM \\
    BGRL~\cite{thakoor2021bgrl} & $X,A$ & 89.69$\pm$0.37 & 93.07$\pm$0.38 & 92.59$\pm$0.14 & 95.48$\pm$0.08 \\
    MVGRL & $X,A$ & 87.52$\pm$0.11 & 91.74$\pm$0.07 & 92.11$\pm$0.12 & 95.33$\pm$0.03 \\
    \midrule
    SGRL~\cite{he2024sgrl} & $X,A$ & 90.23$\pm$0.03 & 93.95$\pm$0.03 & 94.15$\pm$0.04 & 96.23$\pm$0.01 \\
    GBGCL w/o BTCM & $X,A$ & 89.97$\pm$0.08 & 93.87$\pm$0.07 & 93.83$\pm$0.06 & 96.21$\pm$0.03 \\
    \textbf{GBGCL (full)} & $X,A$ & \textbf{90.31$\pm$0.06} & \textbf{94.02$\pm$0.05} & \textbf{94.22$\pm$0.05} & \textbf{96.28$\pm$0.02} \\
    \bottomrule
  \end{tabular}
  \vspace{-0.5em}
\end{strip}

\textbf{Full GBGCL} achieves the best accuracy on every dataset, improving over BGRL by up to 1.53\,pp and over SGRL by 0.05--0.09\,pp.
The parallel branch alone (\emph{w/o BTCM}) matches or slightly trails SGRL, confirming that meso-granular gains require encoder-level injection rather than auxiliary losses alone.

\subsection{Model Analysis}

\textbf{Mechanism diagnostics.}
Without BTCM, Amazon Photo exhibits $\cos(h,z_{\mathrm{new}})\approx 0.9996$ and \texttt{h\_z\_diff} $\approx$ 2--4\%, indicating that parallel write-back barely rotates node embeddings.
Enabling BTCM raises \texttt{h\_z\_diff} to 8--12\% and lowers $\cos(h,z_{\mathrm{new}})$ to $\approx$0.992, showing that meso-scale ball context now perturbs the encoder pathway that feeds downstream probes.
Ball purity remains stable ($\sim$90.7\%) across both variants.

\textbf{Ablation.}
As shown in Table~\ref{tab:abl}, component ablations on Amazon Photo (700 ep, best grid hyperparameters) confirm that BTCM drives the improvement.
\begin{center}
  \footnotesize
  \captionof{table}{Ablation on Amazon Photo (accuracy \%, 700 epochs).}
  \label{tab:abl}
  \begin{tabular}{lc}
    \toprule
    Variant & Acc. \\
    \midrule
    SGRL-equiv.\ (no \texttt{use\_gb}) & 93.50 \\
    GBGCL w/o BTCM (best hp) & 93.87 \\
    \quad + feature concat & 93.38 \\
    \quad + online residual & 92.86 \\
    \quad + target enhance & 93.54 \\
    \quad + larger ball loss & 93.53 \\
    \textbf{GBGCL full (+BTCM)} & \textbf{94.02} \\
    \bottomrule
  \end{tabular}
\end{center}
Removing BTCM (\emph{w/o BTCM}) recovers the weak parallel-branch behavior (93.87\%); naive fusion strategies (feature concat, online residual) perform worse still.
Full GBGCL (+BTCM) yields +0.15\,pp over \emph{w/o BTCM} and +0.52\,pp over SGRL-equiv., validating BTCM as the operative mechanism.

\textbf{Discussion.}
The progression SGRL-equiv.\ $\rightarrow$ GBGCL w/o BTCM $\rightarrow$ GBGCL full mirrors the design rationale: meso-granular structure is informative only when routed into convolution rather than confined to auxiliary branches.
Gains over SGRL are modest (0.05--0.09\,pp) yet consistent across datasets, which is expected near the homophilous benchmark ceiling.
Computers benefits most (+0.08\,pp vs.\ SGRL), aligning with SGRL's own finding that topology constraints matter most on that graph.
Future work includes ball-level RSM (BRSM) and ball-neighbor InfoNCE positives for further meso-scale contrast.

%======================================================================
\section{Conclusion}
\label{sec:conclusion}

In this paper, we extend scattering-based graph representation learning with meso-granularity via granular balls and ball-aware convolution.
Building on SGRL's RSM and TCM, GBGCL couples ball-graph diffusion and ball-level losses with BTCM, which injects diffused ball embeddings into both encoders through BallConv message passing.
Experiments on four benchmarks show that full GBGCL consistently surpasses classic GCL baselines and slightly exceeds SGRL, while ablations attribute the improvement to BTCM rather than parallel write-back alone.
Future work will explore ball-level scattering (BRSM) and contrastive positives defined over ball-neighbor pairs.

\clearpage
\bibliographystyle{IEEEtran}
\bibliography{refs}

\end{document}
