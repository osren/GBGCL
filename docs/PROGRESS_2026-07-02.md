# 进度交接 — 2026-07-02 深夜

> 远程回家前快照。明天回来 `tail` 一眼就知道昨晚停在哪里。
> 配套阅读：[`RESULTS_2026-07-02.md`](RESULTS_2026-07-02.md) · [`RUNBOOK_NEXT_2026-07-02.md`](RUNBOOK_NEXT_2026-07-02.md) · [`SERVER_RUNBOOK.md`](SERVER_RUNBOOK.md)

---

## 一、TL;DR

- **Phase W 已经成功启动**，第一组 `photo_0.05_50x3` 在 16:12:58 写入 journal.csv（OK）
- 当前正在跑：`photo_0.1_50x3` Trial 1/3
- 今晚修了一个**关键 bug**：`common.sh` 的 `--data_dir` 旗标从相对路径回退到绝对路径；c70aa95 的修复被无意中回退，今晚用 sed 在服务器侧恢复（commit `5ba194f` 留在本地，未推到 origin）
- **未启动**：Phase R、BTCM 实施、BRSM

---

## 二、服务器现况（明天 SSH 进来直接看这里）

| 项 | 值 |
|----|----|
| 服务器路径 | `/home/ubuntu/workplace/tc2/GBGCL-main` |
| 用户 | `ubuntu@two`，conda env `CoBF` |
| Phase W PID | `547334`（bash 父进程）/ `547346`（python train.py 实进程） |
| 当前日志 | `logs/nohup/phaseW_20260702_160545.out`（实时 tail） |
| 结果隔离目录 | `results/phaseW_ball_weight/` |
| 历史 out（保留） | `phaseW_20260702_153740.out`（10× FAIL pre-fix）·`phaseW_20260702_155220.out`（用户手动跑过一次 Photo 完成） |

### 2.1 几个重启终端后的查看命令

```bash
cd /home/ubuntu/workplace/tc2/GBGCL-main
source scripts/phases/common.sh
echo "DATA_DIR=${DATA_DIR}"  # 应是 /home/ubuntu/workplace/tc2/GBGCL-main/datasets

# 进程是否还活着
ps -p $(cat logs/nohup/phaseW.pid) && echo "✓ bash 父进程" || echo "✗ 已退出"
ps aux | grep "python train.py" | grep -v grep   # 应该看到 1 条
# 还有 GPU 占用
nvidia-smi | head -20
```

### 2.2 journal.csv 进度（统计）

```bash
echo "Phase W 当前状态:"
grep phaseW logs/phases/journal.csv | awk -F, 'NR>1{print $4, $7}' | tail -15
echo ""
echo "OK 数 / FAIL 数:"
grep phaseW logs/phases/journal.csv | awk -F, 'NR>1{ok+=$7=="OK"} END{print ok" OK"}'
grep phaseW logs/phases/journal.csv | awk -F, 'NR>1{ok+=($7=="FAIL")} END{print ok" FAIL"}'
```

期望（明早）：
- OK 数 = 1 + 已跑完的 0.05/0.1/0.2/... 组合数（大致凌晨 1–2 点 Photo 全部跑完）
- FAIL 数 = 9（pre-fix 残留，不会再涨）

### 2.3 每个组合的实际结果

```bash
head -1 results/phaseW_ball_weight/Photo_summary.csv  # 列名
grep -E "use_gb=1|0\.05|0\.1|0\.2|0\.5|1\.0" results/phaseW_ball_weight/Photo_summary.csv | head -10
```

判读依据（RunBOOK §二.3）：
- 与 Stage-B Top-1（Photo 93.71%, detach/dot/α=0.3/β=0.2/K=10/w=topo+center）对照
- clf_mean 比现有 ≥ +0.3pp → 重跑 Stage-B 用新权重
- 所有差异 ≤ 0.1pp → 推进 BTCM

---

## 三、几个挂账

### 3.1 本地 commit `5ba194f` 没推到 origin

```bash
git log --oneline -3
# 期望：
# 5ba194f fix(common.sh): 恢复 DATA_DIR 绝对路径...
# 21676b4 chore: 提交 RESULTS_2026-07-02.md + 清理无关 Node 残留
# f2709db feat: 下一步实验手册 + Stage W/R/B 脚本...
```

`git push` 失败：Windows 本机 → github.com 被防火墙挡。如果你的家里网络通：

```bash
# 明天在 Windows 本机
cd F:/GBGCL
git push origin main
# 服务器侧随后可弃用 sed 改动，对齐 commit：
#   cd /home/ubuntu/workplace/tc2/GBGCL-main
#   git checkout scripts/phases/common.sh   # 丢掉 sed 改动
```

如果仍推不动 — 没事，服务器 working tree 已正确，commit `5ba194f` 只是历史问题，不影响实验产出。

### 3.2 Phase 0.05 SKIP 逻辑没生效（不阻塞）

我的 `phaseW_ball_weight.sh` 第 24 行的 `grep -q ",0.05,${EPOCHS},"` 没匹配上现有 CSV 行（可能格式是 `0.050,` 或 `,0.05,700,`）。结果 0.05 重跑了 3 trial，得到 50ep 数据 — 这不算浪费，反而是 Stage-B 100% 0.05/700 行的「轻量对照」。

如果想修，prepend 数字宽度：
```bash
grep -q ",${w},${EPOCHS}," "${RESULTS_DIR}/${dataset}_summary.csv"
# 改成固定宽度（w 总是 0.05/0.10/0.20/0.50/1.00）：
grep -qE ",(0\.05|0\.10|0\.20|0\.50|1\.00),${EPOCHS}," "${RESULTS_DIR}/${dataset}_summary.csv"
```

下次部署阶段脚本再改。

### 3.3 Phase R 还没启

`phaseR_repeat_validation.sh` 已 commit（f2709db），服务器已可运行。Photo/Computers 各 700ep×5 trials = 800 epoch 总耗时约 1–2 小时/组 = **整 R 阶段 4–8 小时**。

启动命令（明早看完 W 状态决定启不启）：

```bash
cd /home/ubuntu/workplace/tc2/GBGCL-main
export RESULTS_DIR=$PWD/results/phaseR_repeat
mkdir -p "$RESULTS_DIR"
# W 已用 0 卡主力，可尝试 0 卡串行避免互抢；或用 1 卡单机：
CUDA_VISIBLE_DEVICES=1 bash scripts/phases/run_phase_nohup.sh R
```

记得先看 `nvidia-smi` 确认 GPU 占用 — 如果 Phase W 还在跑，**不要同时启动 R**，等 W 跑完再起。

---

## 四、明日衔接清单（按优先级）

1. **[5 min]** 跑 §2.1–2.3 的 `ps aux` / `journal.csv` / `head Photo_summary.csv` 三条，确认 W 健康
2. **[10 min]** 等下一个组合进 journal（`grep phaseW journal.csv | tail -3`，看时间戳 5–10 min 内有没有更新），如果有 → 一切正常，去睡
3. **[5 min]** 评估：W 还差几组？要不要等 W 全部跑完再启动 R？
4. **[可选]** 写 BTCM (`--gb_btcm`) PR — RunBOOK §四.2 的补丁模板 + §四.3 review 检查表
5. **[可选]** `git push` 推 5ba194f（如网络通了）

---

## 五、紧急 contact / 风险

| 风险 | 触发 | 处理 |
|------|------|------|
| Phase W 进程死了 | 早上 `ps -p 547346` 退码 | 看 `phaseW_20260702_160545.out` 末尾是否 `FAIL`；如非脚本错误，重启 `bash scripts/phases/run_phase_nohup.sh W`，但 **注意**：CSV 已写过行的 checkpoint 会被尊重（train.py 自身有 resume 日志），重跑部分会续上 |
| GPU OOM | 50ep×3 trial 中途进程被 kill | `nvidia-smi` 看显存；5000MB 以下调小 `--gb_rebuild_every` 或切到 CPU 临时验证 |
| 0.05/0.5/1.0 数据散度大 | 5 个 trial 中有任何 NaN/Inf | `grep -c "nan\|inf\|NaN" logs/phases/phaseW/*.log`；通常归因于 `ball_loss_weight` 过高，记录到 `analysis/ball_weight_winners.csv` 时标 SKIP |

---

## 六、改动过的本地文件（备份索引）

未提交的：本文 `docs/PROGRESS_2026-07-02.md` 自身（要 commit 时一并提交）

已 commit（含本会话）：
| Commit | 内容 |
|--------|------|
| `5ba194f` | common.sh 修复 DATA_DIR 绝对路径 |
| `21676b4` | RESULTS_2026-07-02.md + .gitignore 加 Node 排除 |
| `f2709db` | RUNBOOK_NEXT + phaseW/R/B 脚本 + run_phase_nohup case W/R/B |
